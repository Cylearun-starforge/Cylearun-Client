{
  "name": "main",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite build && electron dist/index.cjs",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint . --ext .js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "electron": "latest"
  },
  "devDependencies": {
    "@types/node": "^16.11.36",
    "tsconfig": "workspace:*",
    "eslint-config-custom": "workspace:*"
  }
}
