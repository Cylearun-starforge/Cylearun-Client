{
  "name": "main",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite build && electron dist/index.js",
    "build": "vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint . --ext .js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path ../../.gitignore"
  },
  "dependencies": {
    "ajv": "^8.11.0",
    "electron": "19.0.7",
    "electron-log": "^4.4.8",
    "preload": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^16.11.36",
    "config": "workspace:*",
    "electron-devtools-installer": "^3.2.0",
    "eslint": "^8.18.0",
    "eslint-config-custom": "workspace:*"
  }
}
